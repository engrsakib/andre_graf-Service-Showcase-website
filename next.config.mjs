/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['plus.unsplash.com', 'images.unsplash.com'], // Added images.unsplash.com to the allowed domains
  },
};

export default nextConfig;
